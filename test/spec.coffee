should = require 'should'
require 'coffee-script/register'
require '../app/models/being_model.coffee'
describe "Writing Node with CoffeeScript", ->
  it "is easy to get started testing... or is it?", -> true
